import React, { useEffect } from "react";
import { getArticle } from "../apis/apiActions";

const testParams = {
  date: "2023-05-20",
  keyword: "samsung",
  countries: ["CANADA", "RUSSIA"],
  page: 0,
};

const Home = () => {
  return <div>home</div>;
};

export default Home;
